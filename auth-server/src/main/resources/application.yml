spring:
  application:
    name: auth-server
  security:
    user:
      name: user
      password: password
    oauth2:
      authorizationserver:
        client:
          gateway-client:
            registration:
              client-id: "gateway-client"
              client-secret: "{noop}secret"
              client-authentication-methods:
                - "client_secret_basic"
              authorization-grant-types:
                - "authorization_code"
                - "refresh_token"
              redirect-uris:
                - "http://127.0.0.1:8080/login/oauth2/code/gateway-client"
              post-logout-redirect-uris:
                - "http://127.0.0.1:8080/"
              scopes:
                - "openid"
                - "profile"
            require-authorization-consent: true
server:
  port: 9000